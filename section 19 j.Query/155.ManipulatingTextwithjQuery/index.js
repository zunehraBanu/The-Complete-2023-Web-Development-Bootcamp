$("h1").text("Bye");
$("button").text("Don't click me");
/* In javaScript we were using innerHTML and using innerHTML we were able to not just update text but we were able to add HTML and you can do the same in lesscode using jQuery like this: */
$("button").html("<em>Bye</em>");